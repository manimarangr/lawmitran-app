# Screenshots

Drop captured PNG/WebP screenshots of the running app here, using the filenames listed in
[../screenshots.md](../screenshots.md) (e.g. `home.png`, `lawyer-dashboard.png`,
`admin-moderation.png`). They render inline from that gallery.

Until real captures exist, the `sample-ui/` HTML mockups are the visual reference.
